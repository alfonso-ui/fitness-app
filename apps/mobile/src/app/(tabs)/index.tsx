import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="LiftFlow"
      description="Home will show your next recommended workout and a quick way to start training. Coming in the next milestone."
    />
  );
}
