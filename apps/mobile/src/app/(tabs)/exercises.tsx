import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ExercisesScreen() {
  return (
    <ScreenPlaceholder
      title="Exercises"
      description="The exercise library — search, filter by muscle group or equipment, and view demonstrations."
    />
  );
}
