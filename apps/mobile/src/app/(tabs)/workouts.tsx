import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function WorkoutsScreen() {
  return (
    <ScreenPlaceholder
      title="Workouts"
      description="Your workout templates and the workout builder will live here."
    />
  );
}
