import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Profile"
      description="Account, preferences, and settings will live here once authentication is built."
    />
  );
}
