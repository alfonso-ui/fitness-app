{
  "name": "@fitness-app/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Design tokens and (later) shared UI components. Token values are provisional until branding is approved.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react-native": "*"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
